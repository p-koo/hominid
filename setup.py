from setuptools import setup, find_packages


setup(
    name="hominid",
    version="0.0.0",
    packages=find_packages(),
    description = "A Python package that contains interpretable deep neural networks for regulatory genomcis. ",
    python_requires=">=3.6",
    install_requires=[],
)
